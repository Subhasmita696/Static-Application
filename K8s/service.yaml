apiVersion: v1
kind: Service
metadata:
  name: user-app-service
  namespace: user-app
spec:
  type: LoadBalancer
  selector:
    app: user-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80